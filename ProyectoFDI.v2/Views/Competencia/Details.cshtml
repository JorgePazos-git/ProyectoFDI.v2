@model ProyectoFDI.v2.Models.Competencium

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>

<h1>DETALLES</h1>

<div>
    <h4>Competencia</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Nombre
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NombreCom)
        </dd>
        <dt class = "col-sm-2">
            Fecha de Inicio
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.FechaInicioCom)
        </dd>
        <dt class = "col-sm-2">
            Fecha de Fin
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.FechaFinCom)
        </dd>
        <dt class = "col-sm-2">
            Categoría
        </dt>
        <dd class = "col-sm-10">
            @for (var i = 0; i < ViewBag.ListadoCategorias.Count; i++)
            {
                if (Model.IdCat == (ViewBag.ListadoCategorias[i]).IdCat)
                {
                    <p>
                        @((ViewBag.ListadoCategorias[i]).NombreCat)
                    </p>
                }
            }
        </dd>
        <dt class = "col-sm-2">
            Género
        </dt>
        <dd class = "col-sm-10">
            @for (var i = 0; i < ViewBag.ListadoGeneros.Count; i++)
            {
                if (Model.IdGen == (ViewBag.ListadoGeneros[i]).IdGen)
                {
                    <p>
                        @((ViewBag.ListadoGeneros[i]).NombreGen)
                    </p>
                }
            }
        </dd>
        <dt class = "col-sm-2">
            Juez
        </dt>
        <dd class = "col-sm-10">
            @for (var i = 0; i < ViewBag.ListadoJueces.Count; i++)
            {
                if (Model.IdJuez == (ViewBag.ListadoJueces[i]).IdJuez)
                {
                    <p>
                        @((ViewBag.ListadoJueces[i]).NombresJuez)
                        @((ViewBag.ListadoJueces[i]).ApellidosJuez)
                    </p>
                }
            }
       </dd>
        <dt class = "col-sm-2">
            Modalidad
        </dt>
        <dd class = "col-sm-10">
            @for (var i = 0; i < ViewBag.ListadoModalidades.Count; i++)
            {
                if (Model.IdMod == (ViewBag.ListadoModalidades[i]).IdMod)
                {
                    <p>
                        @((ViewBag.ListadoModalidades[i]).DescripcionMod)
                    </p>
                }
            }
        </dd>
        <dt class = "col-sm-2">
            Sede
        </dt>
        <dd class = "col-sm-10">
            @for (var i = 0; i < ViewBag.ListadoSedes.Count; i++)
            {
                if (Model.IdSede == (ViewBag.ListadoSedes[i]).IdSede)
                {
                    <p>
                        @((ViewBag.ListadoSedes[i]).NombreSede)
                    </p>
                }
            }
        </dd>
        <dt class="col-sm-2">
            Estado
        </dt>
        <dd class="col-sm-10">
            @if(Model.ActivoCom == true){
                 <p>ACTIVO</p>
            }else{
                 <p>INACTIVO</p>
            }
        </dd>
    </dl>
    <table class="table">
        <thead class="table-light">
            <tr>
                <th>
                    Puesto
                </th>
                <th>
                    Resultado de Clasificación
                </th>
                <th>
                    Resultado Final
                </th>
                <th>
                    Competencia
                </th>
                <th>
                    Deportista
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.DetalleCompetencia)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Puesto)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ClasRes)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FinalRes)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.NombreCom)
                    </td>
                    <td>
                        <p>
                            @Html.DisplayFor(modelItem => item.IdDepNavigation.NombresDep) @Html.DisplayFor(modelItem => item.IdDepNavigation.ApellidosDep)
                        </p>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<br />
<div>
    <button type="button" class="btn btn-outline-warning"><a class="boton" asp-action="Edit" asp-route-id="@Model?.IdCom">Editar</a></button>
    <button type="button" class="btn btn-outline-danger"><a class="boton" asp-action="Index">Regresar</a></button>
</div>

